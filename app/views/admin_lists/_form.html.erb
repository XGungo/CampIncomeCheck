<%= form_with(model: admin_list, local: true) do |form| %>
  <% if admin_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_list.errors.count, "error") %> prohibited this admin_list from being saved:</h2>

      <ul>
      <% admin_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :channel_id %>
    <%= form.text_field :channel_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
